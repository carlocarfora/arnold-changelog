# Claude Code Instructions

## Git workflow

Always run `git pull --rebase` before committing and pushing, to avoid rejection from remote changes (the GitHub Actions workflows regularly commit automated changelog updates).
